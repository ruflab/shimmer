name: website

on:
  release:
    types:
      - published

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11.4'
      - name: Load cached Poetry install
        uses: actions/cache@v3
        id: cached-poetry
        with: 
          path: ~/.local
          key: poetry-0
      - name: Install Poetry
        if: steps.cached-poetry.outputs.cache-hit != 'true'
        uses: snok/install-poetry@v1
      - name: Load cached venv
        id: cached-poetry-dependencies
        uses: actions/cache@v3
        with:
          path: .venv
          key: venv-${{ runner.os }}-${{ steps.setup-python.outputs.python-version }}-${{ hashFiles('**/poetry.lock') }}
      - name: Install dependencies
        if: steps.cached-poetry-dependencies.outputs.cache-hit != 'true'
        run: |
          poetry install --with test,dev,docs --without torch
          poetry run pip install torch --index-url https://download.pytorch.org/whl/cpu
          poetry run pip install lightning
      - name: Create new branch
        run: |
          git config user.name "GitHub Actions Bot"
          git config user.email "<>"
          git checkout -b "vesion_docs/${{ github.event.release.tag_name }}"
      - run: poetry run python ./docs/make.py ${{ github.event.release.tag_name }}
      - name: Make PR for new version doc build
        run: |
          git add docs/api
          git commit -m "Build release docs"
          gh pr create --base main --head --title "[DOCS] shimmer API docs ${{ github.event.release.tag_name }}" --body "Automatically generated docs for this release."
        env:
            GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
